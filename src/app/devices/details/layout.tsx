import DeviceDetailsShell from './DeviceDetailsShell';

export default function DeviceDetailsLayout({ children }: { children: React.ReactNode }) {
  return <DeviceDetailsShell>{children}</DeviceDetailsShell>;
}
