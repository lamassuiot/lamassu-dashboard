[request_definition]
r = sub, obj, act

[policy_definition]
p = rel, sub_type, obj_type, act, eft

[role_definition]
g = _, _, _  # sub, obj, rel

[matchers]
m = g(r.sub, r.obj, p.rel) && (r.act == p.act || p.act == "*")
