[changelog]
header = ""
body = """
{% if version %}\
<a name="{{ version }}"></a>
## {% if previous.version %}[{{ version }}](https://github.com/lamassuiot/lamassu-dashboard/compare/{{ previous.version }}...{{ version }}){% else %}{{ version }}{% endif %} ({{ timestamp | date(format="%Y-%m-%d") }})
{% else %}\
## Unreleased
{% endif %}

{% for entry in commits | commit_groups(groups=commit_parsers_groups) %}
### {{ entry.group | striptags | trim | upper_first }}

{% for commit in entry.commits %}
{% if commit.breaking %}* **BREAKING:** {{ commit.message | upper_first }}
{% if commit.breaking_description and commit.breaking_description != commit.message %}  * {{ commit.breaking_description | upper_first }}
{% endif %}
{% else %}* {{ commit.message | upper_first }}
{% endif %}
{% endfor %}

{% endfor %}
"""
trim = true
render_always = true

[git]
conventional_commits = true
filter_unconventional = false
commit_preprocessors = [
    { pattern = '\(#([0-9]+)\)', replace = "([#${1}](https://github.com/lamassuiot/lamassu-dashboard/pull/${1}))" },
]
commit_parsers = [
    { message = "^feat", group = "Features" },
    { message = "^fix", group = "Bug Fixes" },
    { message = "^style", group = "Styling" },
    { message = "^revert", group = "Reverts" },
    { message = "^remove", group = "Removed" },
    { message = "^security", group = "Security Fixes" },
    { message = "^test", group = "Tests" },
    { message = "^chore", group = "Chores" },
    { message = "^refactor", group = "Refactor" },
    { message = "^docs", group = "Documentation" },
    { message = "^build", group = "Build" },
    { message = "^ci", group = "Continuous Integration" },
    { message = ".*", group = "Other" },
]
tag_pattern = "^[0-9]+\\.[0-9]+\\.[0-9]+$"
sort_commits = "oldest"